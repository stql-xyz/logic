/* client/pages/star/star.wxss */
.page {
  background: var(--page-bg);
  height: 100vh;
  color: var(--theme-color);
}
/* 滚动底部样式 */
.bottom-tip {
  height: 160rpx;
  padding-top: 60rpx;
  line-height: 30px;
  text-align: center;
  color: var(--font-color-1);
}