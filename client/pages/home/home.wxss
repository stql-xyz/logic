/* client/pages/home/home.wxss */
.page {
  margin: 0 var(--space-2);
}
.category-list {
  display: flex;
  flex-direction: column;
  align-items: center;
}
.category {
  width: 60vw;
  padding: 2vw;
  line-height: 100%;
  text-align: center;
  margin: var(--space-2);
  background: var(--font-yellow);
}